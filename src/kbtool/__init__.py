"""kbtool — ClaudeKB build/validate toolchain.

Source of truth lives ONLY in the blueprint repo (D15). Each KB vendors a
built wheel of this package; it never depends on the blueprint at run time.
"""

__version__ = "0.3.0"
