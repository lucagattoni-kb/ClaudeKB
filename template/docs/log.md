# Change log

Append-only record of changes to this KB. Newest entries at the bottom. Never
edit earlier entries — this file uses a git union merge driver so concurrent
sessions can both append without conflict, which is only safe for appends.

Entry format:

`## <YYYYMMDD HH:MM> <operation> | <one-line summary>` then a short body.
