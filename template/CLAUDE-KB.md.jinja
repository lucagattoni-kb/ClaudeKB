# {{ kb_title }} — KB-specific guidance

KB-owned (seeded once, never overwritten by upgrades). Edit this freely; it is
imported by `CLAUDE.md`.

## What this KB is about

{{ kb_description }}

_Expand this: scope, what belongs here vs. not, the audience._

## Page types

Defined in `vocab.yml`. When to use each in this KB:

- **article** — long-form structured knowledge (the default).
- **reference** — lookup material (tables, specs, glossaries).
- **concept** — a single idea or entity.
- **playbook** — a procedure to execute.

_Adjust the vocabulary in `vocab.yml` and document the rationale here._

## KB-specific rules

_Naming conventions, section structure, tagging discipline, anything an agent
writing here should know._
